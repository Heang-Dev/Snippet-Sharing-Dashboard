# =============================================================================
# NGROK TESTING ENVIRONMENT EXAMPLE
# =============================================================================
# Copy these settings to your .env file when testing with Ngrok.
# See docs/NGROK-SETUP.md for full documentation.
# =============================================================================

# App Configuration
# Keep APP_URL as your local Valet URL - the TrustNgrokProxy middleware
# will automatically detect and handle Ngrok requests.
APP_ENV=local
APP_DEBUG=true
APP_URL=http://snippet-g11.test

# -----------------------------------------------------------------------------
# Session Configuration
# -----------------------------------------------------------------------------
# SESSION_DOMAIN=null is CRITICAL for Ngrok to work!
# This allows the session cookie to be set for any domain.
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_DOMAIN=null
SESSION_SECURE_COOKIE=
SESSION_SAME_SITE=lax

# -----------------------------------------------------------------------------
# Sanctum Configuration (Optional)
# -----------------------------------------------------------------------------
# These are auto-configured for local environment, but you can override:
# SANCTUM_STATEFUL_DOMAINS=*.ngrok-free.app,*.ngrok.io,localhost,snippet-g11.test

# -----------------------------------------------------------------------------
# Social Auth (OAuth)
# -----------------------------------------------------------------------------
# NOTE: OAuth redirect URIs need to match what's configured in the provider.
# Option 1: Update these to your Ngrok URL for testing OAuth
# Option 2: Leave as local URL (OAuth won't work via Ngrok)

# Google OAuth
# GOOGLE_REDIRECT_URI=https://YOUR-NGROK-URL.ngrok-free.app/auth/google/callback
GOOGLE_REDIRECT_URI=http://snippet-g11.test/auth/google/callback

# GitHub OAuth
# GITHUB_REDIRECT_URI=https://YOUR-NGROK-URL.ngrok-free.app/auth/github/callback
GITHUB_REDIRECT_URI=http://snippet-g11.test/auth/github/callback

# =============================================================================
# QUICK START
# =============================================================================
# 1. Copy relevant settings to your .env file
# 2. Run: php artisan config:clear
# 3. Run: npm run build (IMPORTANT - don't use npm run dev for Ngrok!)
# 4. Run: valet share (or ngrok http snippet-g11.test:80)
# 5. Share the Ngrok URL with your team
# =============================================================================
